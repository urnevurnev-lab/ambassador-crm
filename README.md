# Ambassador CRM: Hookah & Tobacco Edition

Этот проект представляет собой **Telegram Mini App (TWA)** бизнес-класса, разработанное специально для амбассадоров и торговых представителей в индустрии кальянов и табака. Система реализует концепцию "Technical Excellence" с глубокой интеграцией нативных возможностей Telegram.

---

## 🏗 Архитектура проекта

Проект разделен на две основные части:
1.  **Frontend (`/frontend`)**: React-приложение (Vite), стилизованное под премиальный интерфейс iOS/Telegram с использованием Tailwind CSS и Framer Motion. 
2.  **Backend (`/backend`)**: Высокопроизводительный API на базе NestJS, использующий Prisma ORM для работы с PostgreSQL.

---

## 📱 Telegram SDK Integration (v8.0+)

Проект использует последние возможности **Official Telegram Mini App SDK** для создания бесшовного пользовательского опыта:

### 1. Нативные контроли (Native UI)
- **`BackButton`**: Глобальное управление нативной кнопкой «Назад». Автоматически появляется на глубоких уровнях навигации и скрывается на главном экране.
- **`ClosingConfirmation`**: Защита от случайного закрытия приложения свайпом вниз.
- **`MainButton`**: Интеграция для подтверждения ключевых действий (отправка заказа, завершение визита).

### 2. Тактильная отдача (Haptic Feedback)
- **Impact Transitions**: Легкая вибрация при переходах и нажатии кнопок.
- **Notification Feedback**: Сигналы успеха (Success) при отправке данных и ошибки (Error) при отказе в доступе.

### 3. Специальные команды (Bot Messaging)
Система обрабатывает сообщения в чате бота для технической настройки:
- **`ID группы` / `/id`**: Бот мгновенно присылает Chat ID группы (необходимо для настройки уведомлений дистрибьюторам).
- **`мой ID` / `/myid`**: Бот присылает личный Telegram ID пользователя (нужен админу для регистрации сотрудника в CRM).

---

## 📂 Функциональные модули

### 1. Dashboard & Smart Alerts
- **Forgotten Venues**: Список заведений, где не было визитов более 7 дней.
- **Rejected Orders**: Уведомления о заказах, отклоненных дистрибьютором.
- **New Venues**: Верификация новых точек, добавленных в базу.

### 2. Work Hub: Визиты и Заказы
- **Action-Oriented UI**: Кнопка «Начать работу» с выбором сценария: *Транзит (чек-ин), Дегустация, B2B визит, Смена*.
- **Order Wizard**: Трехэтапный сбор заказа с расчетом веса (кг), анализом "маст-листа" и выбором дистрибьютора.
- **KPI / Моя активность**: Раздел с историей всех визитов, фильтрацией по типам активностей и календарем.

### 3. Профиль и Администрирование
- **Smart Profile**: Информация о роли сотрудника и его Telegram ID. Прямой переход в админ-панель для пользователей с ролью `ADMIN`.
- **Admin Panel**: Управление базой заведений, дистрибьюторами, каталогом продукции и Базой Знаний.
- **Security Check**: Приложение блокируется ("Locked"), если Telegram ID пользователя не найден в базе данных CRM.

---

## 🛠 Технологии

- **Frontend**: React 18, Tailwind CSS, Framer Motion, Lucide Icons, Axios.
- **Backend**: NestJS, Prisma, PostgreSQL (Docker).
- **Design Concepts**: iOS-style Glassmorphism, 3D Isometric Icons, Safe-Area Adaptive Layout.

---

## 🚀 Быстрый старт

1.  **Инфраструктура**: `docker-compose up -d` (запуск PostgreSQL).
2.  **Бэкенд**:
    ```bash
    cd backend
    npm install
    npx prisma db push
    npm run start:dev
    ```
3.  **Фронтенд**:
    ```bash
    cd frontend
    npm install
    npm run dev
    ```
4.  **Туннель (для Telegram)**: Рекомендуется использовать `serveo.net` или `ngrok` для проброса порта `5173`.

---

## 🛡 Безопасность

- **Access Control**: Доступ разрешен только сотрудникам, добавленным администратором. Автоматическая регистрация отключена.
- **Data Integrity**: Валидация `WebApp.initData` на стороне бэкенда для защиты API от несанкционированных запросов.
- **Admin Lock**: Дополнительная защита админ-панели (Пин-код/Биометрия).
